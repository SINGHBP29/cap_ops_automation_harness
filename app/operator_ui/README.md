# Operator UI

This package owns the operator-facing console logic.

Use it for code that:

- assembles the `/operator-console-data` payload
- maps approval state into UI state
- applies operator query overrides for incident and shadow views

Current entry point:

- [console_service.py](/Users/bhsingh/Documents/Capstone_Demo3/app/operator_ui/console_service.py)
